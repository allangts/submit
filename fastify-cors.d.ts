declare module 'fastify-cors' {
  // Declare as exportações do módulo aqui, se necessário.
  // Por exemplo, se o módulo exportar uma função, você pode declará-la assim:

  export default function fastifyCors(options?: any): any;
}
